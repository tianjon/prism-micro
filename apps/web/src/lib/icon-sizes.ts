/**
 * 图标尺寸常量。
 * 统一全局图标大小，避免同一功能在不同组件中使用不同尺寸。
 */

export const ICON_SIZE = {
  /** 10px — 极小装饰性图标（下拉箭头、状态点） */
  xs: 10,
  /** 12px — 紧凑行内图标（列表项图标、小按钮图标） */
  sm: 12,
  /** 14px — 默认图标尺寸（按钮图标、表单图标、提示图标） */
  md: 14,
  /** 16px — 标准独立图标（工具栏、导航次级图标、toast 图标） */
  lg: 16,
  /** 18px — 强调图标（侧栏主导航、槽位类型图标） */
  xl: 18,
  /** 20px — 大型图标（侧栏导航图标、卡片标题图标） */
  "2xl": 20,
  /** 24px — 超大图标（空状态、错误状态） */
  "3xl": 24,
} as const;
